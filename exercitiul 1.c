#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x,x[i],max,min, i=1,2,...,n;

    scanf("%d",&x);
    scanf("%d",&x[i]);
      max==x[1];
      for (i=2;i<=n;i++)
      {
          if(x[i]>max)
          {
              max==x[i];
          }

      }
      min==x[1];
      for (i=2;i<=n;i++)
      {
          if(x[i]<min)
          {
              min==x[i];
          }
      }
     printf("max+min=%d\n", max+min);
    return 0;
}
